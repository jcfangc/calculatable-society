[package]
name = "components"
version = "0.1.0"
edition = "2021"

[dependencies]
num = { version = "*", features = ["serde"] }
once_cell = "*"
tracing = "*"
validator = "*"
proce_macro = { path = "../proce_macro" }
types = { path = "../types" }
