[package]
name = "context"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "*", features = ["runtime-tokio-rustls", "postgres"] }
dotenvy = "*"
tokio = { version = "*", features = ["sync"] }
tracing = "*"
async-trait = "*"
once_cell = "*"
